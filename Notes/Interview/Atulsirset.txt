1)How many unused keywords in JAVA ? and which is/are that keywords?(atulsir q1)
ans: There are two unused keyword in JAVA which are const and goto.

2) Which is the only applicable modifire for local variable is?(atulsir q2)
Ans:final


3)Which component is responsible to optimize bytecode to machine code?(atulsir q3)
ans:JIT. 
JIT optimizes bytecode to machine specific language code by compiling similar
   bytecodes at the same time. This reduces overall time taken for 
    compilation of bytecode to machine specific language.

4)…………is a feature that allows one interface to be used for a general class of actions.(atulsir q4)
ans: Polymorphism

5) How can we identify whether a compilation unit is class or interface from a .class file?(atulsir q5)
ans: Java source file header

6)Once an object is created we cannot change its content is called ________ class.(atulsir q6)
ans:"Immutable class"
 "Immutable class" means that once an object is created, we cannot change its content.
In Java, all the wrapper classes (like Integer, Boolean, Byte, Short) and String 
class is immutable. We can create our own immutable class as well.

7) _____ is used to find and fix bugs in the Java programs.(atulsir q7)
ans:The Java Debugger (JDB or jdb) is a command-line java debugger that debugs the java class.

8)How many maximum arguments can be passed to main()?(atulsir q8)
ans: infinite

9)The string is defined in ……… namespace.(atulsir q9)
ans: java.Lang

10)If a variable is declared final, it must be………………….(atulsir q10)
ans: initialize to a value.

extra question:
1)In which memory location does local variable are store?(atulsir extra q1)
ans: stack Memory

2) when we compile nested local inner class then how many .class file generated and which are they?(atulsir extra q2)
Ans : 2. 
One for outer class and another for local inner class

